services:
  - type: web
    name: arith-backend
    env: docker
    plan: free
    branch: main
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO
    dockerfilePath: ./Dockerfile
    envVars:
      - key: TOGETHER_API_KEY
        sync: false
      - key: PORT
        value: 10000

